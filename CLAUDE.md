# CLAUDE.md

This project is shared with other coding agents — notably qwen3-coder, run via the Codex CLI against an Ollama instance externalized on another PC. Codex CLI reads `AGENTS.md` at the repo root natively; to keep instructions in one place across tools instead of drifting copies, `AGENTS.md` is the canonical source.

Read [AGENTS.md](AGENTS.md) in full before doing anything else in this repo — Claude Code does not load it automatically the way it loads this file, so it must be read explicitly, not just referenced.
